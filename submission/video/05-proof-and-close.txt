The newest valid memory wins. Recall Lease blocks the publish action and emits a content-addressed receipt: agent instance, query hash, memory-set digest, evidence count, and final decision. The same receipt is written to private S3 storage for a short audit window. CockroachDB gives the agent durable recall. Recall Lease adds durable revocation, so restarting an agent cannot resurrect permission that no longer exists.
